# Handoff location (i.e. where the files are written to).
handoff:
  buffer: /data/buffer
  holding: /data/holding

# Ingest service location (i.e. where the files will be transferred to)
endpoint:
  user: mxk
  host: lsst-dev03.ncsa.illinois.edu
  buffer: /data/buffer
  staging: /data/staging

# Logging options.
#logging:
# file: transd.log
# level: WARNING

# General options.
#general:
#  chunk_size: 1
#  timeout: None
#  transfer_pool: 1
#  pause: 1
